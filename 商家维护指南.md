# 商家商品维护指南

## 当前方式：修改数据文件（最简单）

商品数据已经抽离到独立文件，你只需编辑这一个文件即可更新网站上的所有商品。

**文件位置**：`app/data/products.ts`

### 添加一个新商品

在 `products` 数组里复制一段现有商品，修改内容即可：

```typescript
{
  id: 9,                          // 编号不能重复，递增即可
  name: "你的商品名称",
  category: "ANIME REPLICA",      // 分类：ANIME REPLICA / MOVIE REPLICA / AUTHENTIC / ACCESSORY
  price: 299,                     // 售价（数字，不要写 $）
  originalPrice: 349,             // 原价（不打折就写 null）
  rating: 4.8,                    // 评分 1.0 ~ 5.0
  reviews: 50,                    // 评价数量
  tag: "NEW",                     // 标签：BESTSELLER / LIMITED / POPULAR / PREMIUM / GIFT / NEW / null
  tagColor: "bg-gold",            // 标签颜色：bg-gold / bg-gold-dark / bg-crimson / bg-crimson-light / null
  image: "/images/products/你的图片.jpg",  // 图片路径（先用占位，后面替换）
  slug: "你的商品-url-标识",       // URL 用的小写英文标识，如 "naruto-sasuke-katana"
  inStock: true,                  // 是否有库存：true / false
},
```

### 修改现有商品

直接找到对应的商品块，改数字或文字即可。比如把价格从 `329` 改成 `299`。

### 删除商品

直接把那一整段花括号 `{}` 删掉即可（注意删掉后面多余的逗号）。

### 标记售罄

把 `inStock: true` 改成 `inStock: false`，页面上会自动显示 "SOLD OUT" 遮罩。

---

## 修改后如何生效

1. 保存 `app/data/products.ts` 文件
2. 开发模式下页面会自动刷新（http://localhost:3000）
3. 正式部署需要先 `npm run build`，然后把 `out/` 目录的内容上传到你的服务器/CDN

---

## 图片管理

目前商品图用的是抽象刀光占位效果。要换成真实图片，有两种方式：

### 方式 A：本地图片（推荐小规模）

1. 把商品图片放到 `public/images/products/` 目录下
2. 在 `products.ts` 里把 `image` 字段改成 `"/images/products/你的图片.jpg"`
3. 支持格式：jpg、png、webp

### 方式 B：图床/云存储（推荐大规模）

1. 把图片上传到 Cloudinary / 阿里云 OSS / AWS S3 等
2. 在 `products.ts` 里把 `image` 字段改成完整的 URL，如 `"https://你的cdn.com/image.jpg"`

---

## 长期维护方案（按业务规模选择）

### 方案 1：JSON 文件管理（适合 1~50 个 SKU）

当前就是这种方式，改一个 TypeScript 数据文件。零成本，不需要数据库。

**优点**：简单、免费、速度快  
**缺点**：每次改商品需要改代码文件，无法在线编辑

### 方案 2：Notion / Airtable + API（适合 50~200 个 SKU）

把商品数据放到 Notion Database 或 Airtable 里，网站通过 API 拉取数据。

- **Notion**：免费，有中文，适合内容管理
- **Airtable**：更像数据库，支持公式计算库存

**优点**：非技术人员也能在线管理商品  
**缺点**：需要写 API 集成代码，首次搭建需要开发时间

### 方案 3：Headless CMS（适合 200+ SKU）

使用专门的内容管理系统：

| CMS | 价格 | 特点 |
|-----|------|------|
| **Strapi** | 开源免费 | 自托管，功能强大，适合技术团队 |
| **Sanity** | 免费起步 | 实时协作，开发者友好 |
| **Contentful** | 免费起步 | 企业级，稳定可靠 |

**优点**：专业商品管理、多用户权限、媒体库、富文本  
**缺点**：需要学习成本，可能有月费

### 方案 4：Shopify + Next.js（完整电商方案）

把 Next.js 作为**前端（Storefront）**，Shopify 作为**后端（商品、订单、支付、库存）**。

- 商品在 Shopify 后台管理
- 订单、支付、物流都在 Shopify 处理
- Next.js 只负责展示页面，通过 Shopify Storefront API 读取数据

**优点**：完整的电商能力，无需自建订单系统  
**缺点**：Shopify 有月费（$29/月起），交易抽成

---

## 推荐演进路线

```
现在（0~50 SKU）     →  改 products.ts 文件
中期（50~200 SKU）   →  接入 Notion / Airtable API
成熟期（200+ SKU）   →  接入 Shopify / Strapi CMS
```

---

## 常用字段速查

tag 可选值：
- `"BESTSELLER"` 热销
- `"LIMITED"` 限量
- `"POPULAR"` 热门
- `"PREMIUM"` 高端
- `"GIFT"` 礼品
- `"MASTERPIECE"` 大师之作
- `"NEW"` 新品
- `null` 不显示标签

tagColor 可选值：
- `"bg-gold"` 金色
- `"bg-gold-dark"` 深金色
- `"bg-crimson"` 暗红色
- `"bg-crimson-light"` 亮红色
- `null` 默认
